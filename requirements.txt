# ---------------------------------------------------------------------
# Zero-Shot Voice Cloning System - Python Dependencies
# ---------------------------------------------------------------------

# --- Web framework / serving ----------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # Required for FastAPI UploadFile/Form parsing
streamlit>=1.33.0                # Testing UI

# --- Deep learning / audio backends ----------------------------------
torch>=2.1.0
torchaudio>=2.1.0
librosa>=0.10.1
noisereduce>=3.0.0
soundfile>=0.12.1

# --- Speech-to-text (ASR) --------------------------------------------
openai-whisper>=20231117

# --- Zero-shot TTS (F5-TTS or XTTS-v2) -------------------------------
# NOTE: F5-TTS is under active development. If the PyPI version is
# unavailable, install from source:
#   pip install git+https://github.com/SWivid/F5-TTS.git
f5-tts>=0.1.0 ; python_version >= "3.9"

# Coqui XTTS-v2 (uncomment if you prefer XTTS over F5-TTS)
# TTS>=0.22.0

# --- Utilities --------------------------------------------------------
numpy>=1.24.0
scipy>=1.11.0
requests>=2.31.0
pyngrok>=7.0.0                  # Colab GPU server tunnel
pydantic>=2.5.0

